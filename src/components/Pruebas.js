import React from "react";

const Pruebas = () => {
    const arrStr = ["1", "3", "5", "9"];
    const nuevo = arrStr.map((i) => Number(i));
    console.log(nuevo);
  return <></>;
};

export default Pruebas;
